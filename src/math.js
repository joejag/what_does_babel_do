const sumAll = (a, b) => { return a + b }
const subtractAll = (a, b) => { return a - b }
const divideAll = (a, b) => { return a / b }
const multiplyAll = (a, b) => { return a * b }

export default { sumAll, subtractAll, divideAll, multiplyAll }
